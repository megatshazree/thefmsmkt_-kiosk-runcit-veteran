import React from 'react';
import KioskButton, { KioskButtonProps } from '../common/KioskButton';

// This is a placeholder. You might want to extend KioskButton or create a new one.
const SettingsButton: React.FC<KioskButtonProps> = (props) => <KioskButton {...props} />;
export default SettingsButton;